# 📘 python-basics

파이썬 기본 문법과 데이터 처리 기초를 학습하기 위해 작성한 실습 프로젝트입니다.
구글 코랩 환경에서 변수, 자료형, 조건문, 반복문, 함수, 파일 입출력, Pandas 기반 데이터 분석 등
Python 입문자가 반드시 익혀야 할 핵심 기능을 정리했습니다.


## 📌 프로젝트 개요

| 항목    | 내용                                  |
| ----- | ----------------------------------- |
| 목적    | Python 기본 문법과 데이터 처리 능력 습득          |
| 환경    | Google Colab                        |
| 주요 기술 | Python, Pandas, Matplotlib          |
| 산출물   | Python 기초 코드, 간단한 데이터 분석 예제, 시각화 실습 |


## 🧩 학습 내용

### ✔ 1. Python 기본 문법

* 변수와 자료형(int, float, str, bool)
* 리스트, 딕셔너리 등 자료구조 활용
* 조건문(if, elif, else)
* 반복문(for, while)
* 함수 정의 및 호출

### ✔ 2. 파일 입출력(File I/O)

* 텍스트 파일 생성 및 읽기
* `with open()` 문법 사용

### ✔ 3. Pandas 기초

* DataFrame 생성 및 다루기
* 컬럼 선택·정렬·필터링
* describe(), info() 등 요약 메서드 활용
* 새로운 컬럼 추가
* 간단한 데이터 전처리

### ✔ 4. Matplotlib 기초 시각화

* 막대 그래프(bar)
* 선 그래프(line)
* 히스토그램(histplot)


## 🔍 예시 코드

### 변수, 자료형

```python
num = 10
name = "Hyeyoung"
pi = 3.14
is_active = True

print(num, name, pi, is_active)
```

### 리스트 & 딕셔너리

```python
fruits = ["apple", "banana", "orange"]
student = {"name": "Hyeyoung", "age": 23}
```

### 조건문

```python
score = 85
if score >= 90:
    print("A")
elif score >= 80:
    print("B")
else:
    print("C")
```

### 반복문

```python
for fruit in fruits:
    print(fruit)
```

### 함수

```python
def add(a, b):
    return a + b
```


## 📊 **Pandas 예제**

```python
import pandas as pd

data = {
    "name": ["A", "B", "C"],
    "score": [90, 85, 70]
}

df = pd.DataFrame(data)
df.describe()
```


## 📈 시각화 예제

```python
import matplotlib.pyplot as plt

plt.bar(df["name"], df["score"])
plt.title("Score by Student")
plt.show()
```


## 📁 Colab Notebook

* python-basics.ipynb → (GitHub 업로드 후 링크 연결하세요)
* 노션 포트폴리오에도 링크 연결 가능


## 🚀 프로젝트를 통해 배운 점

* Python 문법을 실습 중심으로 이해하게 되었음
* Pandas를 활용해 데이터를 불러오고, 선택·정렬·필터링하는 흐름 습득
* 데이터 분석 프로젝트(mini-project-eda)와 머신러닝 실습(ai-model-basic)으로 확장할 수 있는 기반 확보
